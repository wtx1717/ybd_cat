# Top Nav TODO

## 未完成内容

- 顶部导航栏整条黑色背景框目前改为 CSS 绘制，不再使用 `assets/images/top-nav/nav-frame.png`。
- 当前 CSS 方案只是临时替代，用于避免原背景素材边框过粗、缩放后不够美观的问题。
- 后续需要重新制作正式导航背景素材，建议做成更细边框版本，或拆成九宫格资源：四角固定、边线拉伸、中间纯色填充。

## 后续素材要求

- 边框线条应比当前 `nav-frame.png` 更细。
- 中间深色纹理不要压过图标和文字。
- 横向拉伸时边线厚度不能变化。
- 不要把可变文字烘焙进背景图，文字应尽量由 HTML/CSS 渲染。
